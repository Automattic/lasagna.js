name: Unit Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: npm install

      - name: TypeScript Check
        run: npm run lint

      - name: Jest Tests
        run: npm test
